package distsys26.local_eoc.sensor_reading_processor;

// Superseded by SensorProcessor (@Service) + AlertPublisher (@Service).
// Kept as a stub so the file does not need to be deleted from git.
class ResultGenerator {
    void generate() {}
}
